import React, { useState } from 'react';
import BookingForm from "./BookingForm";

const Booking = () => {
    return (
        <main>
            <BookingForm/>
        </main>
    )
}

export default Booking;